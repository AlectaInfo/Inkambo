<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Sign in &middot; Twitter Bootstrap</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="">
        <meta name="author" content="">

        <link href="{{asset('bundles/abcstyle/css/tweeterbootstrap/bootstrap.css')}}" rel="stylesheet">
        <link href="{{asset('bundles/abcstyle/css/tweeterbootstrap/bootstrap-responsive.css')}}" rel="stylesheet">
        <!--<link href="{{asset('bundles/mainlogin/css/parsley/parsley.css')}}" rel="stylesheet">-->

        <style type="text/css">
                body {
                    padding-top: 40px;
                    padding-bottom: 40px;
                    background-color: #f5f5f5;
                }

                .form-signin {
                    max-width: 300px;
                    padding: 19px 29px 29px;
                    margin: 0 auto 20px;
                    background-color: #fff;
                    border: 1px solid #e5e5e5;
                    -webkit-border-radius: 5px;
                    -moz-border-radius: 5px;
                    border-radius: 5px;
                    -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                    -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                    box-shadow: 0 1px 2px rgba(0,0,0,.05);
                }
                .form-signin .form-signin-heading,
                .form-signin .checkbox {
                    margin-bottom: 10px;
                }
                .form-signin input[type="text"],
                .form-signin input[type="password"] {
                    font-size: 16px;
                    height: auto;
                    margin-bottom: 15px;
                    padding: 7px 9px;
                }

            </style>
        </head>

        <body>
            <div class="container">

                <form class="form-signin" method="POST" action="{{path('abc_home_homepage')}}" data-validate="parsley">
                    <h2 class="form-signin-heading">Please Log in</h2>
                    <input type="text" class="input-block-level" name="Username" placeholder="Username" data-trigger="change" data-required="true">
                    <input type="password" class="input-block-level" name="Password" placeholder="Password" data-trigger="change" data-required="true">
                    <br>
                    <label class="checkbox">
                        <input type="checkbox" name="Remember" value="remember-me"> Remember me
                    </label>
                    <br>
                    <button class="btn btn-large btn-primary" type="submit">Sign in</button>
                    <br><br>
                </form>

            </div>
    {%if name is defined%}
            <div class="alert-info fade in">
                <strong>{{name}}</strong>
            </div>
    {%endif%}

            <script src="{{'asset(bundles/abcstyle/js/tweeterbootstrap/jquery.js'}}"></script>
            <script src="{{'asset(bundles/abcstyle/js/tweeterbootstrap/bootstrap.js'}}"></script>
            <!--<script src="{{'asset(bundles/mainlogin/js/parsley/parsley.min.js'}}"></script>
            <script src="{{'asset(bundles/mainlogin/js/parsley/parsley-standalone.min'}}"></script>
            <script src="{{'asset(bundles/mainlogin/js/parsley/parsley.extend.min'}}"></script>-->

        {%block javascripts %}              
        {%endblock%}
        </body>
    </html>

{% extends "ABCStyleBundle:Templates:TwitterFormTemplate.html.twig"%}

{% block container %}
    <h1>Resource Person Edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('rsp') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

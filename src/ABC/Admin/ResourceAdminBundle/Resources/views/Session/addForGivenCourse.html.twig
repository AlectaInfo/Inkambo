{% extends "ABCStyleBundle:Templates:TwitterFormTemplate.html.twig"%}

{% block container%}
         
        {{ form_start(form, {'action': path('session_createGiven',{'courseId':id,'id':id2})})}}
         <h3 style="color: #0088cc; text-align: center">Add Session for {{id}}</h3><br>
        {{form_errors(form)}}
        {{ form_row(form.sessionId) }}
        {{ form_row(form.year) }}
        {{ form_row(form.startMonth) }}
        {{ form_row(form.class) }}
        {{ form_row(form.timeslot) }}
        {{form_widget(form.submit)}}
        <ul class="record_actions">
         <li>
         <a href="{{ path('session_showAll') }}">
            Back to the list
        </a>       
             </li>  
         </ul>
{% endblock %}        

{% extends "ABCStyleBundle:Templates:TwitterFormTemplate.html.twig"%}

{% block container%}
<script>
    function popitup(url) {
	newwindow=window.open(url,'name','height=600,width=400');
	if (window.focus) {newwindow.focus()}
	return false;
    }
</script>    


        {{ form_start(form)}}
        {{ form_row(form.deptName)}}

<input type="button" value="Create New Department" onclick="popitup('{{path('updatecourse_new')}}')">  <br>

   <!-- {#    <div class="record_actions">
            <a href="{{ path('courseadmin_home') }}">
                Add New Department
            </a>       
        
        <br>
         </div>#}-->
<div>
        <p> Please enter the course id excluding the course code</p>
</div>
        {{ form_row(form.courseId)}}
        {{ form_row(form.title)}}
        {{ form_row(form.duration)}}
        {{ form_row(form.fee)}}

        {{ form_row(form.description)}}
        {{ form_rest(form)}}
{{ form_end(form) }}

<div class="record_actions">

    <a href="{{ path('courseadmin_home') }}">
        Back to the list
    </a>       

</div>
{% endblock %}        


{#{%for course in courses%}

    {{course.course_id}} - {{course.title}}  <input type="button" value="Create Session" onclick="popitup('{{path('session_new')}}')">  <br>

{%endfor%}#}